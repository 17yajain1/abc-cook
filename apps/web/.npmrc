# Vite 8 is new enough that some plugins (@vitejs/plugin-react, vitest) still declare
# narrower peer ranges. Their npm peer-resolution also trips an arborist bug on this
# npm version. legacy-peer-deps sidesteps both; revisit once the ecosystem catches up.
legacy-peer-deps=true
